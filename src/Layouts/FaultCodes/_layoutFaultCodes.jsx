import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

import ContainerContent from '../../components/ContainerContent';
import ContentSearchFault from './components/ContentSearchFault';

const LayoutFaultCodes = () => {
  return (
    <ContainerContent>
     <ContentSearchFault />
    </ContainerContent>
  );
};

export default LayoutFaultCodes;
